#include <stdio.h>
#include <string.h>

/**
*puts -entry point
*Return: Always 0 (Success)
*/
int main(void)
{
	char text[] = "Programming is like building a multilingual puzzle\n";

	/*writing to stdout*/
	puts(text);
	return (0);
}
