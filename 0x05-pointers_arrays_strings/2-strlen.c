#include "main.h"

/**
 *_strlen - returns the length of a string
 *@s: Assign string to a pointer
 *
 *Return: return the string length
 */

int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)
	{
		;
	}
	return (n);
}
